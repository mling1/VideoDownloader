# 更新日志

## v1.8.3 (2026-09-09)

### 修复

- **移除油猴脚本元数据中的 AI 痕迹**：`@namespace` 从 `https://doubao.com` 改为 `https://github.com/mling1/VideoDownloader`，避免发布代码中出现 AI 工具官网信息。

---

## v1.8.2 (2026-09-06)

### 修复

- **重构 `seekToBeginning()` 函数：只负责 seek，play 由调用方处理**：之前 `seekToBeginning()` 里的 `onSeeked` 事件监听器和自动下载模式下的 `seeked` 事件监听器冲突，导致自动完整下载标红时重新捕获片段数异常、跳至开头一跳完就标红。改为：
  - `seekToBeginning()` 只负责暂停→清空moof保留ftyp→解除锁定→设置`manualSeekToBeginning`标志→seek到开头
  - play 由调用方在 `seekToBeginning()` 之后直接调用（seek 是异步的，play 会等待 seek 完成）
  - 移除了 `seekToBeginning()` 里的 `onSeeked` 和 `setTimeout` 兜底逻辑
  - 移除了自动下载模式下的 `needDelayPlay` 变量
  - 正常模式下的 `seeked` 事件里保留 `manualSeekToBeginning` 检测，避免跳至开头时标红

---

## v1.8.1 (2026-09-06)

### 修复

- **修复自动完整下载标红时重新捕获片段数异常的问题**：抽离 `seekToBeginning()` 函数时遗漏了原来逻辑中的 `manualSeekToBeginning = false` 先重置步骤，导致事件监听器顺序冲突。补全该步骤，确保和原来自动下载里的逻辑完全一致。

---

## v1.8.0 (2026-09-06)

### 重构

- **抽离共用的 `seekToBeginning()` 函数**：把自动完整下载和跳至结尾按钮里"主动seek到开头"的逻辑抽成共用函数，确保两处跳转逻辑完全一致，避免跳至开头时概率异常标红或片段数异常。
  - 共用流程：暂停→清空moof保留ftyp→解除所有锁定→设置`manualSeekToBeginning`标志→seek到开头→监听seeked事件完成后play→5秒兜底强制play
  - 移除了正常模式`seeked`事件里单独的`manualSeekToBeginning`检测，统一由共用函数管理

---

## v1.7.9 (2026-09-06)

### 修复

- **跳至开头：修复异常直接标红的问题**：之前跳至开头时 `completeLocked` 被重置为 false，导致 `seeked` 事件里的重播检测不触发，而是进入向前跳转标红逻辑。参考自动完整下载的 `manualSeekToBeginning` 标志，在跳至开头时设置该标志，`seeked` 事件里检测到就不标红，直接重置标志并更新位置。

---

## v1.7.8 (2026-09-06)

### 修复

- **跳至开头：重置捕获状态，避免片段数异常**：之前跳至开头时没有重置捕获状态，旧数据和新数据混在一起导致片段数异常（如变成49）。改为跳至开头前调用 `resetCaptureState()` 清空旧数据，片段数重置为0，然后从头开始重新捕获。

---

## v1.7.7 (2026-09-06)

### 修复

- **跳至结尾按钮：点击后立即更新按钮文字**：之前点击跳至结尾后，需要等视频加载完触发 `timeupdate` 事件，按钮文字才会变成"跳至开头"。改为在 `skipToEnd` 函数里直接更新按钮文字：跳至结尾后立即变为"跳至开头"，跳至开头后立即变回"跳至结尾"。

---

## v1.7.6 (2026-09-06)

### 修复

- **跳至结尾按钮：动态更新按钮文字**：视频播放到结尾时（`ended` 或 `duration - currentTime < 3`），按钮文字自动变为"跳至开头"；离开结尾时自动变回"跳至结尾"。在 `timeupdate` 事件里实时更新，放在 `completeLocked` 判断之前，确保锁定后也能更新。

---

## v1.7.5 (2026-09-06)

### 修复

- **自动完整下载：跳转标红逻辑改成和正常模式一致**：之前自动下载过程中只要用户手动跳转就立即中断并强制标红。改为和正常模式一样的逻辑：向前跳转立即中断并标红；向后跳转只有超出最大缓存end才中断并标红，在缓存内不中断继续自动下载。
- **跳至结尾按钮：修复"已在结尾则跳至开头"不生效的问题**：判断条件从 `duration - currentTime < 2` 改为 `< 3`，同时添加 `video.ended` 检查；跳至开头后自动调用 `video.play()` 开始播放。

---

## v1.7.4 (2026-09-06)

### 修复

- **正常模式：向后跳转标红逻辑移除幅度限制**：之前向后跳转需要幅度大于3秒且超出已缓存部分才标红。改为只要向后跳转且超出最大缓存end就标红，不限制幅度（是否标红只取决于有没有超过已缓存部分）。
- **跳至结尾按钮：修复"已在结尾则跳至开头"不生效的问题**：判断条件从 `duration - currentTime < 1` 改为 `< 2`，更宽松可靠；同时添加 `isFinite(duration)` 检查，防止直播流（duration=Infinity）误判。

---

## v1.7.3 (2026-09-06)

### 修复

- **正常模式：向前跳转标红逻辑彻底修复**：之前外层有个 `> 3` 秒的幅度判断，小幅度向前跳转根本进不了标红逻辑。改为向前跳转不受幅度限制，只要向前跳转就标红；向后跳转才需要幅度大于3秒且超出最大缓存end才标红。
- **跳至结尾按钮：添加"已在结尾则跳至开头"逻辑**：点击跳至结尾时，如果检测到视频已经在结尾（currentTime 与 duration 相差小于1秒），则改为跳至开头，方便快速重播。

---

## v1.7.2 (2026-09-06)

### 修复

- **正常模式：向前跳转标红逻辑改为只要向前跳转就标红**：之前向前跳转只有大幅跳转（>15秒）才标红，小幅跳转不标红（只设置5秒内忽略ftyp清空）。但实测发现重复的向前跳转哪怕只有一点点也会导致片段数异常。改为只要向前跳转就标红锁定，移除了小幅跳转的特殊处理。

---

## v1.7.1 (2026-09-06)

### 修复

- **正常模式：向后跳转判定方案回退到 maxBufferedEnd**：经过实测，`lastBufferedRanges`（timeupdate 记录）方案会导致片段数变少，`maxBufferedEnd`（历史最大缓存 end）方案最稳，不会导致片段数少。回退到 `if (v.currentTime > maxBufferedEnd) needLock = true;` 的判定方式。

---

## v1.7.0 (2026-09-06)

### 修复

- **正常模式：向后跳转超出缓存范围判定方案最终确定**：经过多轮尝试，最终确定用 `lastBufferedRanges`（timeupdate 事件持续记录的缓存范围）判定向后跳转目标是否在已缓存范围内。
  - `maxBufferedEnd`（历史最大值）→ 只会增加不会减少，seek 清理后仍是旧大值 ❌
  - `lastSeekBufferedRanges`（seek 事件记录）→ seek 触发时 buffered 可能已被清理，导致为空，所有跳转都标红 ❌
  - `lastBufferedRanges`（timeupdate 持续记录）→ 稳定可靠 ✅
- 移除了不再需要的 seek 事件监听和 `lastSeekBufferedRanges` 变量。

---

## v1.6.9 (2026-09-06)

### 修复

- **正常模式：向后跳转超出缓存范围判定不准确**：之前用 `maxBufferedEnd`（历史最大值，只会增加不会减少）不准确；改用 `lastBufferedRanges`（timeupdate 记录）也可能为空或未及时更新。最终方案：添加 **seek 事件**监听，在跳转前（seek 事件触发时，buffered 尚未被清理）记录准确的缓存范围到 `lastSeekBufferedRanges`，然后在 seeked 事件里用这个准确的范围判定跳转目标是否在已缓存范围内，不在则标红。

---

## v1.6.8 (2026-09-06)

### 修复

- **正常模式：向后跳转超出缓存范围不标红**：向后跳转判定用的是 `maxBufferedEnd`（历史最大缓存 end，只会增加不会减少），seek 清理 buffered 后它仍是旧的大值，导致向后跳转超出当前缓存范围时 `v.currentTime > maxBufferedEnd` 判定为 false，不标红。修复：改用 `lastBufferedRanges`（timeupdate 事件记录的 seek 前最后一次准确缓存范围）判定跳转目标是否在已缓存范围内，不在则标红。

---

## v1.6.7 (2026-09-06)

### 修复

- **自动完整下载：下载完成后正常标红逻辑完全失效**：自动下载完成后 `captureLocked = true`（防止合成期间新分片混入），但 seeked 事件处理开头有 `if (captureLocked) return;`，导致下载完成后用户跳转进度条时，重播检测（seek到开头清空旧数据）和正常标红逻辑（向前大幅跳转/向后超出缓存）全部被拦截，完全不执行。修复：移除 seeked 事件里的 `if (captureLocked) return;` 拦截。`captureLocked` 只用于阻止 MSE 捕获新分片，不应该阻止 seeked 事件的状态判断。

---

## v1.6.6 (2026-09-06)

### 修复

- **自动完整下载：暂停后正常模式标红逻辑失效**：自动下载过程中正常播放不会触发 seeked 事件，`vdLastCt` 停留在自动下载开始时的位置（0）。暂停后跳转时，跳转方向判定错误（向前跳转被误判为向后跳转），导致正常标红逻辑失效（向前大幅跳转不标红，向后跳转超出缓存也可能不标红）。修复：暂停时更新 `vdLastCt = video.currentTime`，确保暂停后跳转方向判定正确。注意：这只是更新位置变量，暂停后完全回到正常模式，遵循正常标红逻辑（向前大幅跳转标红，向后跳转超出缓存标红），不会套用自动下载的中断逻辑。

---

## v1.6.5 (2026-09-06)

### 修复

- **自动完整下载：暂停后跳转时标红逻辑判定失效**：自动下载过程中正常播放不会触发 seeked 事件，`vdLastCt` 停留在自动下载开始时的位置（0）。暂停后跳转时，跳转方向判定错误（向前跳转被误判为向后跳转），导致标红逻辑失效（向前大幅跳转不标红，向后跳转超出缓存也可能不标红）。修复：暂停时更新 `vdLastCt = video.currentTime`，确保暂停后跳转时跳转方向判定正确。

---

## v1.6.4 (2026-09-06)

### 修复

- **自动完整下载：暂停后标红逻辑异常**：自动完整下载暂停（取消）时，只恢复了播放状态和视频画面，但**没有标红锁定**，导致暂停后跳转进度条时标红逻辑异常，已捕获的分片可能被继续追加，片段数异常。修复：暂停时和中断一样处理，强制标红锁定（`captureLocked=true`、`completeLocked=true`、`truncatedByGap=true`、`seekedToEnd=true`），按钮显示"自动下载异常(点击重试)"，防止后续追加分片导致异常。

---

## v1.6.3 (2026-09-06)

### 修复

- **自动完整下载：暂停后跳转再重新开始导致片段数异常**：自动完整下载暂停后跳转进度条，再点击重新开始时，代码解除了 `completeLocked`、`truncatedByGap`、`seekedToEnd` 等锁定状态，但**遗漏了 `captureLocked`**。导致 seeked 事件处理里 `if (captureLocked) return` 直接返回，主动 seek 到开头的逻辑不执行，新分片不会被捕获，最终片段数异常。修复：重新开始时同时解除 `captureLocked = false`。

---

## v1.6.2 (2026-09-06)

### 修复

- **m3u8下载：按钮文字未正确居中**：返回按钮和"转码为MP4下载"按钮的文字没有正确居中（返回按钮文字偏右，转码MP4按钮因感叹号导致内容不居中）。改为：所有下载按钮和返回按钮都使用 `display: flex` + `justify-content: center` + `align-items: center` 确保内容水平垂直居中；感叹号的 margin 从 2px 缩小到 1px，减少对居中的影响。

---

## v1.6.1 (2026-09-06)

### 修复

- **m3u8下载：范围下载"返回"按钮异常过宽**：原作者按钮样式是 `box-sizing: content-box`、`font-size: 24px`，导致返回按钮即使设置了宽度也会过宽。改为：强制 `box-sizing: border-box`、`font-size: 11px`、`padding: 8px 0`（左右padding为0）、`max-width: 28px`、`overflow: hidden`，彻底限制按钮宽度为28px。

---

## v1.6.0 (2026-09-06)

### 重构

- **m3u8下载：转码MP4按钮感叹号改为HTML模板直接写死**：之前用 JS 动态添加感叹号，由于 Vue 重新渲染、时序问题、选择器匹配等原因，感叹号经常错误地添加到"原格式下载"按钮上。改为：直接在 HTML 模板里给"转码为MP4下载"按钮写死感叹号（`<span class="vd-mp4-warn">!</span>`），同时添加 `title` 悬停提示。彻底避免 JS 动态添加导致的匹配错误。
- **m3u8下载：范围下载"返回"按钮宽度调整为20px**。

---

## v1.5.9 (2026-09-06)

### 修复

- **m3u8下载：转码MP4按钮感叹号位置错误（彻底修复）**：之前用文本匹配方式选择按钮，Vue 重新渲染后可能匹配到错误按钮（如"原格式下载"）。改为：给"转码为MP4下载"按钮添加特定类名 `mp4-download-btn`，添加感叹号时用 `querySelector('.mp4-download-btn')` 精确选择，彻底避免匹配错误。
- **m3u8下载：范围下载"返回"按钮宽度自适应**：从固定宽度 36px 改为 `width: auto` 自适应宽度，根据"返回"文字内容自动调整，避免过宽或过窄。

---

## v1.5.8 (2026-09-06)

### 修复

- **m3u8下载：转码MP4按钮感叹号位置错误**：之前用 `querySelectorAll('div, button')` 遍历所有嵌套元素，可能导致感叹号添加到错误的按钮（如"原格式下载"）。改为：只遍历 `container.children` 直接子元素，并用 `textContent.trim().startsWith('转码为MP4')` 精确匹配，确保感叹号只添加到"转码为MP4下载"按钮上。
- **m3u8下载：范围下载"返回"按钮宽度**：从 40px 缩小到 36px。

---

## v1.5.7 (2026-09-06)

### 修复

- **m3u8下载：范围下载"返回"按钮显示时机和宽度**：
  1. 返回按钮之前在完整下载模式下也显示（未点击"特定范围下载"就有返回按钮），改为添加 `v-if="rangeDownload.isShowRange"` 条件，只在范围下载模式下显示
  2. 返回按钮宽度从 48px 缩小到 40px

---

## v1.5.6 (2026-09-06)

### 修复

- **m3u8下载：范围下载"返回"按钮位置错误**：之前返回按钮放在起始/截止片段输入框那一行，导致布局混乱且宽度过大。改为：返回按钮放在"原格式下载"和"转码为MP4下载"按钮的同一行（最右边），宽度缩小为 48px，灰色背景，和下载按钮风格一致。

---

## v1.5.5 (2026-09-06)

### 改进

- **m3u8下载：范围下载"返回"按钮位置和宽度优化**：返回按钮从固定宽度 50px 改为自适应宽度（`width: auto`，最小 40px），并用 `margin-left: auto` 推到行的最右边，避免挤压起始/截止片段输入框。

---

## v1.5.4 (2026-09-06)

### 改进

- **m3u8下载：范围下载模式优化**：
  1. 添加"返回"按钮：范围下载展开后，在起始片段/截止片段输入框后面添加灰色"返回"按钮，点击后退出范围下载模式，返回完整下载模式
  2. 下载完成后保持范围下载状态：之前下载完成后会自动重置 `rangeDownload.isShowRange=false` 并清空起始/结束片段，导致用户想再次范围下载时需要重新输入。改为下载完成后不重置范围下载状态，方便用户再次下载相同范围
  3. "返回"按钮样式：灰色背景（#6c757d），白色文字，hover 时变深（#5a6268），宽度 50px

---

## v1.5.3 (2026-09-06)

### 重构

- **m3u8下载：用自己的下载逻辑替换原作者的downloadFile**：原作者的 `downloadFile` 方法存在文件名异常问题（原格式下载文件名只有 `ts`，MP4下载文件名 `mp4.mp4` 重复后缀）。改为：添加自己的 `downloadBlob(fileName)` 方法，把 `mediaFileList` 里的片段拼接为 Blob，用 a 标签下载，文件名完全由自己控制（`fileName + .ts/.mp4`），彻底解决文件名异常问题。同时移除了 `mergeTsToMp4` 方法（不再需要）。

---

## v1.5.2 (2026-09-06)

### 修复

- **m3u8下载：下载文件名异常**：删掉原作者 `<title>` 标签后，原作者代码里 `document.title !== 'm3u8 downloader'` 条件为 true，导致 `fileName` 被错误设置为 `this.getDocumentTitle()`（当前页面标题），下载的文件名变成 `ts`（原格式）或 `mp4.mp4`（MP4重复后缀）。改为：注入前临时设置 `document.title = 'm3u8 downloader'`，注入后恢复原始标题，确保 `fileName` 正常生成（视频标题或时间戳）。

---

## v1.5.1 (2026-09-06)

### 修复

- **m3u8下载：范围下载面板文字超出框**：加了感叹号后，"转码为MP4下载"按钮文字变长，范围下载展开时 5 个元素（主输入框 + 2个范围输入框 + 2个下载按钮）平分宽度，导致按钮文字超出框。改为：范围输入框（起始片段/截止片段）固定宽度 72px，下载按钮（原格式下载/转码为MP4下载）flex 自适应放宽，避免文字超出框。

---

## v1.5.0 (2026-09-06)

### 重构

- **m3u8下载：重构注入样式保护方式**：之前尝试删掉原作者 `<style>` 里的全局样式块，但正则匹配不完整导致弹窗样式丢失。改为：保留原作者完整 `<style>` 标签（确保弹窗样式正常），在注入前保存当前页面的 `body.style.cssText` 和 `html.style.cssText`，注入后立即恢复被污染的全局样式，并用 `setTimeout` 兜底防止 Vue 异步修改。这样既保证弹窗样式正常，又避免污染当前页面。

---

## v1.4.9 (2026-09-06)

### 修复

- **m3u8下载：删掉原作者<style>导致弹窗样式丢失**：上一版直接删掉了原作者的整个 `<style>` 标签，导致弹窗的按钮、输入框、片段列表等必要样式全部丢失，弹窗变白。改为：只删掉原作者 `<style>` 里的全局样式块（`html,`、`body {`、`p {` 等会污染当前页面的选择器），保留按钮、输入框、片段列表等必要样式。既避免全局样式污染，又保证弹窗样式正常。

---

## v1.4.8 (2026-09-06)

### 修复

- **m3u8下载：注入后全局样式污染当前页面**：原作者 HTML 里的 `<style>` 标签包含全局样式（`html,`、`body {`、`p {`），注入到页面后会影响当前网页的样式。改为：注入前同时删掉原作者 HTML 里的 `<title>` 和 `<style>` 标签（我们已经有自己的样式布局 modal，原作者的全局样式是冗余的），避免污染当前页面。
- **m3u8下载：转码为MP4按钮感叹号过大导致弹窗变高**：感叹号字号从 13px 调小到 11px，并设置 `line-height:1`，避免加载时导致弹窗整体高度增加。

---

## v1.4.7 (2026-09-06)

### 重构

- **m3u8下载：重构网页标题保护方式**：之前用 `Object.defineProperty` 临时冻结 `document.title` 防止原作者工具修改网页标题，方式复杂且有副作用。改为：在注入前直接删掉原作者 HTML 里的 `<title>m3u8 downloader</title>` 标签（这才是标题被修改的真正原因），注入后不会再修改网页标题。保留原有的 `history.replaceState/pushState` 保护。代码更简洁干净。

---

## v1.4.6 (2026-09-06)

### 修复

- **m3u8下载：注入后网页标题被修改**：原作者的在线工具在 Vue 初始化时会修改 `document.title`，导致点击 m3u8 下载后网页标题变成工具的标题。改为：注入期间临时冻结 `document.title`（用 Object.defineProperty 拦截 setter），注入后恢复原始标题，并用 setTimeout 兜底防止 Vue 异步修改标题。同时保留原有的 history.replaceState/pushState 保护。

### 改进

- **m3u8下载：转码为MP4按钮感叹号样式调整**：去掉圆形边框，改为纯红色加粗文字感叹号，字号和按钮文字一致（13px），两边都有，垂直居中。

---

## v1.4.5 (2026-09-06)

### 改进

- **m3u8下载：转码为MP4下载按钮添加感叹号和悬停提示**：在"转码为MP4下载"按钮文字后面添加一个橙色上标感叹号，鼠标悬停时显示提示"注意：转码为MP4可能出现时长显示不准确、进度条无法拖动等问题，建议优先使用原格式下载"。按钮宽度保持不变（三个按钮flex平分宽度）。

---

## v1.4.4 (2026-09-06)

### 改进

- **下载标红内容时的弹窗提示更新**：改为"检测到大跳转或视频未从头播放，下载的视频可能出现音画不同步、后半段无声、进度条无法拖拽或画面异常等问题，建议从头播放或使用自动完整下载获取完整视频\n是否继续下载？"，覆盖大跳转和未从头播放两种情况。
- **自动下载异常按钮文字改回原文字，括号改用英文括号**：按钮文字恢复为"自动下载异常(点击重试)"，中文括号（）比英文括号()宽，改用英文括号避免按钮宽度变宽。

---

## v1.4.3 (2026-09-06)

### 改进

- **简化下载标红内容时的弹窗提示**：从"检测到大跳转，下载的视频可能出现音画不同步、后半段无声、进度条无法拖拽或画面异常等问题，建议使用自动完整下载获取完整视频"简化为"检测到大跳转，下载的视频可能出现音画不同步、无声或画面异常等问题，建议使用自动完整下载"，更简洁。
- **缩短自动下载异常时的按钮文字**：从"自动下载异常（点击重试）"改为"下载异常（点此重试）"，避免按钮宽度变宽。

---

## v1.4.2 (2026-09-06)

### 改进

- **自动完整下载：加载完所有片段就直接下载，不用等视频放完**：之前是等视频播放完（ended事件）再等2秒开始下载，视频放完后可能自动重播导致多捕获片段（正常54个变成56个）。改为：自动下载过程中每500ms轮询检测 `isBufferedComplete()`，一旦检测到 buffered 完整（所有片段加载完），等500ms让最后几个分片稳定后直接开始下载。ended事件保留作为兜底（有些网站buffered不会完整，等放完再下载）。下载完成逻辑抽成统一的 `finishAutoDownload()` 函数，轮询和ended事件共用。

### 修复

- 自动下载取消/中断时清除轮询，防止轮询继续运行。

---

## v1.4.1 (2026-09-06)

### 修复

- **自动下载过程中跳转后显示标黄而不是标红**：自动下载过程中用户跳转后，之前是继续走到正常模式的标红逻辑，但如果视频之前已经完整加载（`completeLocked = true`，标黄），正常模式里 `!completeLocked` 条件不满足，导致不会标红还是标黄。改为：自动下载过程中用户跳转中断后，**直接强制标红**（自动下载过程中跳转一定是不完整的），不依赖正常模式的判断。

---

## v1.4.0 (2026-09-06)

### 修复

- **正常播放时点击自动下载立即显示"下载异常"**：自动下载启动时，如果视频已经完整加载（`completeLocked = true`，标黄），会主动 seek 到开头。但 seeked 事件里的判断是 `!manualSeekToBeginning`，主动 seek 时这个标志是 true，导致走到了"用户手动跳转"逻辑，自动下载被误中断。改为：主动 seek 到开头（`manualSeekToBeginning = true`）或重播时，都不中断自动下载，继续捕获。

---

## v1.3.9 (2026-09-06)

### 修复

- **自动下载按钮文字颜色变成黑色**：`btnAuto.style.color = ''` 把 baseBtn 里的 `color: #fff`（白色）覆盖成空字符串，导致文字变成默认黑色。去掉所有按钮颜色/边框重置代码，保持 baseBtn 的白色字体。
- **自动下载异常中断时未恢复画面和静音**：用户手动跳转中断自动下载时，只恢复了播放速度，没有恢复视频的显示和静音状态。改为和暂停逻辑一样，恢复视频的静音和隐藏状态（不暂停，让视频继续播放）。

---

## v1.3.8 (2026-09-06)

### 修复

- **自动下载按钮字体颜色被误改**：回退按钮文字颜色和边框颜色的修改，保持原来的白色字体。自动下载被用户手动跳转中断后，按钮文字改为「自动下载异常（点击重试）」，不改变颜色。

---

## v1.3.7 (2026-09-06)

### 修复

- **下载标红片段时文件名未加"片段"后缀**：之前的判断逻辑是 `completeLocked || isBufferedComplete()` 就不加后缀，但标红时 `completeLocked = true`（锁定捕获），导致标红片段也不加后缀。改为：标红（`seekedToEnd || truncatedByGap`）时即使 `completeLocked = true` 也加"片段"后缀；只有标黄/完整锁定且未标红才不加后缀。

---

## v1.3.6 (2026-09-06)

### 修复

- **视频不是从头开始播放时标红判断不准确**：回退向后跳转的 `lastBufferedRanges` 逐段判断（该方案问题更大），改回 `maxBufferedEnd` 判断。新增：视频刚加载（canplay事件）时检测是否从头播放，如果 `currentTime > 3秒`（不是从头开始），直接标红锁定，避免后续跳转判断不准确导致捕获异常。

---

## v1.3.5 (2026-09-06)

### 修复

- **正常模式下向后跳转标红判断不准确**：之前用 `maxBufferedEnd`（历史最大缓存end值）判断向后跳转目标是否在已缓存范围内，如果视频不是从头开始播放（缓存范围从中间开始），判断会不准确。改为用 `lastBufferedRanges`（seek前最后一次记录的完整缓存范围）逐段判断跳转目标是否真的在已缓存范围内，只有跳转到缓存范围外才标红。

---

## v1.3.4 (2026-09-06)

### 修复

- **自动完整下载过程中用户手动跳转导致重复片段/片段不足**：上一版在自动下载模式下仍做复杂的标红判断（向前>15秒或向后超出缓存才标红中断），容易误判导致捕获异常。改为：自动下载模式下，**只要用户手动跳转就立即中断自动下载**（不做跳转幅度判断），然后回到正常模式由原有逻辑处理标红。视频播放完自动重播（seek到开头）不中断，继续自动下载。中断后按钮显示「自动下载已中断」（红色）。

---

## v1.3.3 (2026-09-06)

### 改进

- **自动下载被标红中断后按钮状态提示**：自动完整下载过程中用户手动大跳转触发标红锁定并中断自动下载后，按钮文字改为「自动下载已中断」，文字和边框变为红色，让用户清楚知道自动下载已被取消。再次点击启动自动下载或主动取消时恢复正常按钮样式。

---

## v1.3.2 (2026-09-06)

### 修复

- **自动完整下载过程中用户手动拖动进度条不会标红锁定**：之前自动下载模式下 seeked 事件直接 return，跳过了标红锁定逻辑，导致用户手动跳转后捕获的片段异常且不会锁定。改为：自动下载模式下，用户手动大跳转（向前>15秒或向后超出已缓存范围）会触发标红锁定，并**自动中断自动下载**（恢复播放速度、移除监听、恢复视频显示）；重播或主动 seek 到开头不标红，继续自动下载；小幅跳转不标红。

---

## v1.3.1 (2026-09-06)

### 修复

- **转码为MP4下载无法播放**：上一版尝试用单例 Transmuxer 逐片段 push 统一转码来解决时长问题，但 mux.js 的 Transmuxer 不适合多次 push，导致输出文件无法播放。回退到逐片段独立转码 + `keepOriginalTimestamps: false`（该配置可正常出画面播放），转码后的 fMP4 数据用原作者 downloadFile 拼接下载。

### 已知限制

- 转码为MP4下载受原作者 mux.js 逐片段独立转码架构限制，**可能存在时长显示不准确、进度条无法拖动的问题**。建议优先使用「原格式下载」（.ts 文件，可正常播放、可拖动、时长正确），需要 MP4 格式时可用 FFmpeg 等工具二次转码。

---

## v1.3.0 (2026-09-06)

### 修复

- **转码为MP4下载时长显示13小时、无法拖动进度条**：根因是原作者 mux.js 逐片段独立转码，每个片段独立创建 Transmuxer 导致时间戳都从0开始，拼接后总时长计算错误。改为：`conversionMp4` 直接返回原始 ts 数据（不逐片段转码），所有片段下载完成后在 `mergeTsToMp4` 里用**单例 Transmuxer 逐片段 push** 统一转码，确保时间戳连续、时长正确、可拖动进度条。
- **转码为MP4下载文件名重复 .mp4 后缀**：原作者 `downloadFile` 会根据 `isGetMP4` 自动添加后缀，手动又加了一次导致 `xxx.mp4.mp4`。改为在 `mergeTsToMp4` 里手动触发下载，精确控制文件名。

---

## v1.2.9 (2026-09-06)

### 修复

- **转码为MP4下载画面白色（有声音无画面）**：上一版尝试直接改后缀（不转码），但 Chrome 根据文件后缀选择解码器，`.mp4` 后缀用 MP4 解码器解码 TS 内容会导致视频轨道解码失败（音频 AAC 在两种封装里一样所以有声音）。改回 mux.js 逐片段转码（`keepOriginalTimestamps: false` + 移除 duration 参数），该配置之前验证过可以正常出画面。转码后的 fMP4 数据用原作者 downloadFile 拼接下载。

---

## v1.2.8 (2026-09-06)

### 修复

- **MPEG-TS 分片转 MP4 始终只有声音无画面**：mux.js 转码（无论 keepOriginalTimestamps 为 true 还是 false、逐片段还是整个 ts 一次性转码）都会导致各种兼容性问题（只有一帧卡住、时长显示13小时、有声音无画面等）。改为：「转码为MP4下载」不再做任何转码，直接用原作者的 `downloadFile` 拼接（和原格式下载内容完全一致，已验证可正常播放），仅将文件后缀改为 `.mp4`。Chrome 会根据文件内容自动选择解码器，ts/fMP4 内容用 .mp4 后缀也能正常播放。需要真正的 MP4 格式时可用 FFmpeg 等工具二次转码。

---

## v1.2.7 (2026-09-06)

### 修复

- **MPEG-TS 分片转 MP4 后只有声音无画面**：mux.js 转码配置 `keepOriginalTimestamps: true` 在整个 ts 一次性转码时会导致视频轨道初始化信息异常，改为 `keepOriginalTimestamps: false`（mux.js 重新计算连续时间戳，之前逐片段转码时该配置可正常出画面）。
- **按钮文字改为小写**：「M3U8下载」→「m3u8下载」。

---

## v1.2.6 (2026-09-06)

### 修复

- **fMP4/CMAF 分片转 MP4 后有声音无画面（全屏白色）**：上一版对 fMP4 分片采用手动拼接所有分片的方式，但手动拼接可能导致视频轨道初始化信息不完整。改为：fMP4/CMAF 分片直接调用原作者的 `downloadFile` 方法拼接（和原格式下载完全一致的逻辑，已验证可正常播放），仅将文件后缀改为 `.mp4`。MPEG-TS 分片仍用 mux.js 一次性转码为 fMP4。

---

## v1.2.5 (2026-09-06)

### 修复

- **转码为MP4下载在 fMP4/CMAF 分片网站失败（错误码0）**：vdocipher 等专业 DRM 服务商使用 CMAF（fMP4 分片）而非标准 MPEG-TS，mux.js 的 Transmuxer 只能处理 MPEG-TS，遇到 fMP4 会报错。改为：先检测分片类型（检查前8字节是否为 ftyp box），fMP4/CMAF 分片直接拼接所有分片下载（本身就是标准 MP4 格式），MPEG-TS 分片才用 mux.js 转码。同时改进错误提示，显示具体错误信息而非数字 0。

---

## v1.2.4 (2026-09-06)

### 修复

- **转码为MP4下载合成失败（音频样本提取失败0个样本）**：上一版尝试用脚本内置的 mp4box.js 无损合成逻辑（mergeMP4）对 mux.js 转码出的 fMP4 进行二次合成，但 mp4box.js 解析 mux.js 输出的 fMP4 时存在兼容性问题，导致音频轨道样本提取失败。改为：mux.js 把整个 ts 一次性转码出的 fMP4 本身就是标准的 fragmented MP4 格式（ftyp+moov+moof+mdat），现代播放器（Chrome、Edge、VLC 等）均可正常播放和拖动进度条，直接下载 fMP4 即可，不需要再经过 mp4box.js 二次合成。

---

## v1.2.3 (2026-09-06)

### 新功能

- **转码为MP4下载改用脚本内置的 mp4box.js 无损合成逻辑**：原作者的 mux.js 逐片段转码存在架构性缺陷（每个片段独立创建 Transmuxer，导致时间戳重叠、只有一帧卡住、时长显示13小时、从10秒开始播放、无法拖动进度条等）。现在改为：
  1. `conversionMp4` 直接返回原始 ts 数据，不做 mux.js 逐片段转码
  2. 所有片段下载完成后，把所有 ts 片段拼接成一个完整的 ts 文件
  3. 用 mux.js 把整个 ts 文件一次性转码为一个完整的 fMP4（initSegment + 所有 data）
  4. 调用脚本内置的 `mergeMP4` 函数（mp4box.js 无损合成）把 fMP4 合成为普通 MP4
  5. 合成的 MP4 支持拖动进度条、时长正确、音画同步

### 变更

- **按钮改名**：「在线完整下载」→「M3U8下载」，更准确地反映功能（纯前端 m3u8 下载工具）。

### 修复

- **特定范围下载后再次正常下载从设置位置开始**：下载完成后重置 `rangeDownload.isShowRange=false`、`startSegment=''`、`endSegment=''`，确保第二次下载是全部下载。

---

## v1.2.2 (2026-09-06)

### 变更

- **按钮改名**：「在线完整下载」→「M3U8下载」，更准确地反映功能（纯前端 m3u8 下载工具，非在线完整下载）。
- **移除「转码为MP4下载」按钮**：原作者 mux.js 转码存在架构性缺陷（每个片段独立创建 Transmuxer 转码，导致时间戳重叠、只有一帧卡住、时长显示13小时、从10秒开始播放等问题），且无法通过简单修改配置解决。保留「原格式下载」（.ts 文件，能正常播放、可拖动进度条、时长正确），需要 MP4 格式时可用 FFmpeg 等工具二次转码。

### 修复

- **特定范围下载后再次正常下载从设置位置开始**：下载完成后重置 `rangeDownload.isShowRange=false`、`startSegment=''`、`endSegment=''`，确保第二次下载是全部下载。片段下载进度列表（`tsUrlList`/`finishList`）仍然保持展开。

---

## v1.2.1 (2026-09-06)

### 修复

- **在线工具MP4转码只有一帧卡住**：根因是 `keepOriginalTimestamps: true` 配合"每个片段独立创建 Transmuxer 转码"会导致每个片段的时间戳都从0开始，拼接后所有片段时间戳重叠在0点，播放器认为视频只有一帧。改为 `keepOriginalTimestamps: false` 让 mux.js 重新计算连续时间戳，视频可正常播放。
- **在线工具MP4转码无画面**：原作者转码配置里的 `duration: parseInt(this.durationSecond)` 在 durationSecond 为0或解析不正确时，会导致转码出的 MP4 视频轨道 duration 信息错误，播放器无法渲染画面。移除 duration 参数，由 mux.js 根据实际片段自动计算。
- **在线工具下载时长不稳定/只有几分钟**：下载完成后立即执行 `mediaFileList=[]` 清空数据，但 `downloadFile` 合成文件是异步的，导致部分片段还没写入就被清空。改为 `setTimeout` 延迟1秒再清空，确保合成完成。
- **特定范围下载重复下载**：回退了额外添加的范围校验逻辑（该逻辑干扰了原有的下载流程），完全使用原作者的 `Math.max/Math.min` 自动修正范围。

### 改进

- **下载完成后保留片段面板**：下载完成后不再清空 `tsUrlList`/`finishList`，也不收起范围输入面板，方便用户查看每个片段的下载结果。点击「重置」按钮才会完全清空。
- **代码清理**：删除调试用的 `console.log` 和 `console.warn` 语句，保留错误提示。

### 已知限制

- 在线工具转码为 MP4 后，受原作者"每个片段独立转码"架构限制，moov atom 索引不完整，**可能无法拖动进度条**，时长显示可能不准确。建议优先使用「原格式下载」（.ts 文件，支持拖动，兼容性最好），需要 MP4 格式时可用 FFmpeg 等工具二次转码。

---

## v1.2.0 (2026-09-06)

### 新功能

- **在线完整下载（页面注入模式）**：从跳转模式改为页面注入模式，把原作者 m3u8 在线下载工具的 HTML+JS 注入到当前视频页面底部运行，请求资源时 origin 为视频页面域名，从根本上解决跨域导致的 ts 片段下载失败问题。
- **右下角简洁小弹窗 UI**：在线工具从占满底部改为右下角固定小弹窗（420px 宽），输入框在顶部占满宽度，下面三个按钮平分宽度（特定范围下载、原格式下载、转码为MP4下载），隐藏所有冗余功能（帮助链接、测试链接、特大视频下载、反馈条、跨域按钮、最终下载按钮、二维码社区区域等）。
- **无视频流页面自动停止脚本**：DOM 就绪后检查 video 元素，无则 MutationObserver 监听，30 秒超时设置 `scriptStopped=true`，所有被劫持方法开头检查 scriptStopped 直接调用原始方法透传，避免普通网页的性能影响。
- **m3u8 地址通过 fetch/XHR 劫持捕获**：比检查 video.src 更可靠（Chrome 里 hls.js 把 m3u8 转 fMP4 后 video.src 是 blob:）。

### 修复

- **关闭/重置按钮点击失效**：按钮从 contentArea 移到 modal 层（不随内容滚动），z-index 提高到 100，点击事件加 `e.stopPropagation()`，重置按钮添加兜底逻辑。
- **关闭后重开不自动填链接**：弹窗是隐藏不是销毁，重开时自动把当前捕获到的 m3u8Url 更新到 Vue 实例的输入框。
- **重置不清空输入框**：resetDownload 方法里添加 `this.url = ''`，兜底逻辑里也同步清空。
- **下载格式状态残留**：下载完成后重置 `isGetMP4=false` 和 `rangeDownload.isShowRange=false`，解决"转码MP4后再点原格式下载还是MP4"的问题。
- **混合内容限制（错误码0）**：在线工具地址从 http:// 改为 https://，HTTPS 页面不再拦截 HTTP 请求。
- **页面不能返回上一级**：注入的业务代码含 `window.history.replaceState` 修改页面历史，在 eval 前后临时禁用 history.replaceState/pushState，eval 后恢复。

### 改进

- **在线工具地址**：改用原作者 blog.luckly-mjw.cn 工具，source 参数直接拼接原始 m3u8 地址（工具不做 URL 解码，不能用 encodeURIComponent）。
- **注入成功不弹 alert**：用户要求不弹窗通知。

---

## v1.1.0 (2026-09-05)

### 新功能

- **大跳转标红锁定**：检测到向前大幅跳转（>15秒）或向后跳转超出已缓存范围时，片段数变红色并锁定捕获，防止跳转后加载的不连续内容混入已捕获分片，避免合成后音画不同步。跳至结尾按钮执行前也会直接标红锁定。
- **完整捕获标橙锁定**：视频完整加载后，片段数变橙色并锁定，不再追加新分片，防止自动连播/重播内容混入。
- **自动完整下载可暂停取消**：自动下载过程中再次点击按钮可暂停取消，恢复视频正常播放。
- **自动下载后恢复原倍数**：自动完整下载完成或取消后，恢复用户原来的播放倍数，而不是固定为 1 倍。
- **面板未展开时也能捕获**：页面加载完成即开始扫描视频元素，不依赖面板展开，解决刷新后偶尔捕获不到分片的问题。
- **抖音平台下载当前视频**：多 MediaSource 场景下（抖音会预加载下一个视频）只取最后一组音视频轨，配合 video src 变化检测，确保下载的是当前正在播放的视频而非下一个预加载视频。

### 修复（v1.0.8 已存在的 bug）

- **标黄后下载文件名仍带「片段」后缀**：根因是文件名后缀判断只用了 `isBufferedComplete()`，但下载时 MSE buffered 可能已被清理，导致误判为不完整。改用 `completeLocked || isBufferedComplete()` 判断，标黄时设置的完整锁定会被保留。
- **自动完整下载后倍数停留在 16 倍**：自动下载完成/取消后固定恢复为 1 倍，未记录用户原来的倍数。新增 `originalSpeed` 变量，开始时记录原倍数，完成/取消时恢复。
- **刷新后偶尔捕获不到视频分片**：`scanVideos()` 只在面板展开时调用，面板未展开时 video 元素的事件监听未添加。改为页面加载完成即调用一次 `scanVideos()`。
- **切换视频/重播时继续追加旧内容**：脚本无法识别视频切换或重播，导致新视频分片追加到旧数据后面。新增 video src MutationObserver 变化检测，seeked 事件检测到从头开始（currentTime<3）且已锁定时清空旧数据并解除锁定。
- **向前小幅跳转后片段被意外清空**：向前小幅跳转后播放器重新发送 ftyp（init segment），触发"新流开始清空旧数据"逻辑，导致片段被清空。新增 `smallSeekIgnoreFtyp` 标志，向前小幅跳转时 5 秒内忽略 ftyp 清空，5 秒后自动恢复。
- **自动完整下载捕获过程中误标黄**：16 倍速播放缓冲很快，`isBufferedComplete()` 误判为完整导致 UI 提前标黄并锁定，后续分片不再被捕获。改为 autoDownload 模式下不标红也不标黄，保持白色，等播放完成后再锁定。
- **直接跳至结尾下载音画不同步**：跳至结尾后播放器加载片尾内容，与前面已捕获分片时间戳不连续，合成后音画不同步。新增大跳转标红锁定 + `truncateAtTimeGap()` 截断不连续内容，只保留前面连续的部分。
- **标红后自动完整下载片段不完整/合成失败**：标红后点击自动下载时，seek 到开头的时机不对，导致前面分片被跳过或 ftyp 丢失。修复为先解除锁定再 seek，等 800ms 让播放器完全从 0 重新加载后再 play。
- **合成失败弹窗信息不清晰**：弹窗未显示完整性检测结果和合成失败原因。优化为显示完整性检测结果 + 两级合成结果，合成失败后才询问是否分开下载。

### 改进

- **大跳转下载提示**：检测到大跳转后下载时，弹出确认框提示"下载的视频可能出现音画不同步、后半段无声、进度条无法拖拽或画面异常等问题，建议使用自动完整下载获取完整视频"，用户确认后才继续下载。
- **不完整下载文件名后缀**：大跳转（标红）后下载的文件名加 `_片段` 后缀，完整视频（标橙/正常）不加。
- **自动完整下载静默优化**：16 倍速 + 静音 + 隐藏画面（visibility:hidden，不影响 MSE 拉流），播放完成后等 2 秒让最后几个分片加载完成，再锁定合成。

---

## v1.0.8 (2026-09-04)

### 修复
- **完整加载合成失败**：视频结束时最后一个 moof 分片可能被截断（不完整），导致 mp4box 触发 `onError` 直接 reject 合成失败。而不完整加载时最后一个分片反而是完整的（按分片边界加载），所以能成功。
  - 新增 `truncateIncompleteBox()`：喂给 mp4box 前，从后向前检测最后一个 box 是否完整，不完整就截断到上一个完整 box
  - mp4box `onError` 不再直接 reject，而是在控制台打印警告，用已解析的样本继续完成合成（末尾不完整分片的错误可以忽略）
  - 样本提取失败时错误信息包含解析警告，方便排查

### 新增
- **不完整下载文件名加「片段」后缀**：未完整加载时下载的文件名自动加 `_片段`，区分完整视频和片段视频。覆盖三种下载场景：单轨道下载、合成下载、合成失败后分开下载。

### 改进
- 合成失败弹窗显示更详细的错误信息（包含解析警告）。

---

## v1.0.7 (2026-09-04)

### 新增
- **合成失败自定义弹窗**：替换原生 `confirm()`（只有确定/取消），改为自定义三按钮弹窗：
  - 「分开下载」：分别下载视频和音频文件
  - 「在线完整下载」：跳转在线工具兜底处理
  - 「取消」：关闭弹窗
- 弹窗显示完整性检测结果（已完整加载/未完整加载）和具体错误信息。

### 改进
- `openOnlineTool()` 不再要求必须有 m3u8 地址，没有时直接打开在线工具主页，MSE 流合成失败后也能使用在线兜底。

---

## v1.0.5 (2026-09-04)

### 修复
- **不完整视频分类兜底修复**：修复了视频未完整加载时下载只能分开下载（视频和音频两个文件）、无法合成的问题。
  - 根因：原兜底逻辑要求未分类条目 `unknown.length >= 2` 才触发，但不完整捕获时常见场景是一个轨的 kind 已识别（video，有 mime），另一个轨 kind 为空（刷新后 SourceBuffer 已创建导致 mime 缺失 + init segment 错过），此时未分类条目只有 1 个，兜底不生效，导致音频轨为空、直接走单轨道分开下载。
  - 修复：兜底逻辑改为只要有未分类条目且视频或音频缺失，就按数据总量大小补全（视频通常远大于音频），不再要求 `unknown >= 2`。

### 验证
- 浏览器端到端实测：B 站视频只播放 7.6 秒（不完整加载，32 视频分片 + 34 音频分片），成功合成 3.95MB MP4。
- 合成 MP4 结构验证：`ftyp + moov + mdat` 标准普通 MP4，双轨道 `vide + soun`，非 fragmented（可拖动进度条），音频轨 volume=256（满音量，无声问题已修复）。

---

## v1.0.4 (2026-09-04)

### 修复
- **捕获不稳定**：重构为 document-start 立即劫持 `SourceBuffer.prototype.appendBuffer` 原型方法，不再依赖拦截 `addSourceBuffer`，注入稍晚也能稳定捕获分片。
- **刷新重复捕获**：检测到新的 init segment（ftyp）自动清空旧缓存，切换视频不会新旧分片混杂。
- **没声音**：显式设置音频轨 `tkhd.volume=1`（mp4box addTrack 默认 volume=0 会导致严格播放器静音）。

### 改进
- 自动完整下载时静音 + 隐藏画面，减少资源占用。
- UI 优化：自动完整下载按钮位置调整。

---

## v1.0.0 ~ v1.0.3

- 初始版本：MSE 流媒体捕获 + mp4box 无损合成 + 倍速加速 + 跳至结尾 + 自动完整下载 + 完整性检测 + 在线兜底。
- 逐步修复：文件打不开、无法拖动进度条、弹窗过多、合成失败等问题。
- 定型普通 MP4 合成方案（手动构建 ftyp+moov+单个 mdat，不用 mp4box addSample 因为它出 fragmented MP4）。
